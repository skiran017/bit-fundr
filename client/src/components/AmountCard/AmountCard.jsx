import React from 'react';
import { Flex, Text, Box } from '@chakra-ui/react';
import { daysLeft } from '../../utils/helpers';

function AmountCard({
  owner,
  title,
  description,
  target,
  deadline,
  amountCollected,
  image,
  handleClick,
}) {
  return <div>AmountCard</div>;
}

export default AmountCard;
